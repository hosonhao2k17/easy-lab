name: Easy Lab 

on: 
  push: 
    branches:
      - main 
jobs:
  easy-lab:
    runs-on: ubuntu-latest

    steps:
      - name: Say something 
        run: echo "Hello anbatocom" 
      - name: List Structure 
        run: pwd 
      - name: List directory 
        run: ls 